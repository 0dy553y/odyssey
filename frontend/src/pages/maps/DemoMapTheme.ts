import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff6ad5',
    },
    secondary: {
      main: '#8690ff',
    },
  },
});

export default theme;
